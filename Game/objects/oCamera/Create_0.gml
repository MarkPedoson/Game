//Camera set up
cam = view_camera[0];
follow = oPlayer;	//Centers on player

viewWidthHalf = camera_get_view_width(cam) * 0.5;
viewHeightHalf = camera_get_view_height(cam) * 0.5;
xTo = xstart;
yTo = ystart;
